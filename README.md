en el terminal poner :
npm install
npm install express
npm  install dotenv

para corre el servidor :
node app.js

el port es:
 http://localhost:3000

 un archivo  nombrarlo :
 .env 
 dentro de el PORT = (tu por ej:3333)